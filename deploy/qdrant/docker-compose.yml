services:
  qdrant:
    image: qdrant/qdrant:v1.15.5
    container_name: ai-investment-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
    environment:
      QDRANT__SERVICE__API_KEY: ${QDRANT_API_KEY}
    volumes:
      - ./storage:/qdrant/storage
